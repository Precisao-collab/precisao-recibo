# ============================================
# ARQUIVO .env OTIMIZADO PARA EC2 t3.micro (1 GB RAM)
# ============================================
# 
# ⚠️ AVISO: Esta configuração é para t3.micro (1 GB RAM)
# ⚠️ NÃO é recomendado para produção com alto volume
# ⚠️ Para produção, use t3.small (2 GB) ou superior
#
# INSTRUÇÕES:
# 1. Configure as credenciais SMTP do Gmail abaixo
# 2. Renomeie este arquivo para .env (ou copie o conteúdo)
# 3. Use este arquivo apenas se estiver usando t3.micro
#
# ============================================

# Gmail SMTP Configuration
SPRING_MAIL_USERNAME=seu-email@gmail.com
SPRING_MAIL_PASSWORD=sua-senha-app

# Email Configuration
APP_EMAIL_REMETENTE=seu-email@gmail.com
APP_EMAIL_NOME_REMETENTE=Sistema de Recibos - Precisao

# Frontend URL for CORS
APP_FRONTEND_URL=https://precisao-recibo-frontend.onrender.com

# Backend URL (Elastic IP)
APP_BACKEND_URL=http://3.136.3.86:8080

# Java Options OTIMIZADAS para t3.micro (1 GB RAM)
# -Xmx256m: Heap máximo de 256 MB (reduzido de 512 MB)
# -Xms128m: Heap inicial de 128 MB (reduzido de 256 MB)
# -XX:MaxMetaspaceSize=128m: Limita Metaspace (onde ficam classes)
# -XX:+UseG1GC: Usa G1 Garbage Collector (mais eficiente para baixa memória)
# -XX:MaxGCPauseMillis=200: Limita pausa do GC a 200ms
# -XX:+UseStringDeduplication: Deduplica strings (economiza memória)
# -XX:+OptimizeStringConcat: Otimiza concatenação de strings
JAVA_TOOL_OPTIONS=-Xmx256m -Xms128m -XX:MaxMetaspaceSize=128m -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -XX:+UseStringDeduplication -XX:+OptimizeStringConcat

# API Externa - Empreendimentos (opcional, tem valores padrão no código)
API_EMPREENDIMENTO_NM_SISTEMA=PRE
API_EMPREENDIMENTO_ID_PESSOAFISICA=2091
API_EMPREENDIMENTO_ID_SESSAO=f81b72d3-ab99-4078-9d03-77a4d9051420
API_EMPREENDIMENTO_ID_CHAVEDISPOSITIVO=api

# API Externa - Validação de CPF
API_CPF_VALIDACAO_URL=https://api.receitaws.com.br/v1/cpf

